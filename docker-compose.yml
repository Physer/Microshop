---
services:
  db:
    image: postgres
    container_name: postgres_database
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env

  adminer:
    image: adminer
    container_name: database_admin
    restart: always
    ports:
      - 8080:8080