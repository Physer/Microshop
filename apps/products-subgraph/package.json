{
  "name": "products-subgraph",
  "version": "0.1.0",
  "author": "Alex Schouls",
  "type": "module",
  "scripts": {
    "clean": "shx rm -r dist",
    "lint": "eslint --cache --ext .ts,.json,.graphql",
    "start": "pnpm run build && node ./dist/index.js --inspect",
    "build": "tsc"
  },
  "dependencies": {
    "@apollo/server": "^4.3.2",
    "graphql": "^16.6.0",
    "lowdb": "^5.0.5"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0"
  }
}
