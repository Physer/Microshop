extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Product {
  name: String
  description: String
  id: Int
}

type Query {
  products: [Product]
}

type Mutation {
  seedProductData: Boolean
}
